const { neon } = require('@neondatabase/serverless');
const { drizzle } = require('drizzle-orm/neon-http');
const { users } = require('./apps/falbor/drizzle/schema.ts'); // Wait, drizzls/schema is different
