{
  "name": "falbor-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=falbor",
    "build": "npm run build --workspace=falbor",
    "start": "npm run start --workspace=falbor"
  },
  "dependencies": {
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "@babel/parser": "^8.0.4",
    "decomment": "^0.9.5"
  }
}
